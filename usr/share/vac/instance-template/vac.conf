# Sub-instancia VAC — plantilla de configuración
# Hereda /etc/vac/vac.conf; define aquí solo lo que difiere.
# Sobreescrituras: vac.conf.d/*.conf
# Referencia completa: /usr/share/vac/vac.conf.defaults

# IP o hostname del servidor VAS de esta sub-instancia
VAS_HOST=

# Esquema de conexión: http (default) | https
# VAS_SCHEME=http

# Nivel de log: no | normal | debug (hereda vac.conf si no se especifica)
# LOG_LEVEL=normal
